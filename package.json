{
  "name": "novawear-commerce-suite",
  "version": "1.0.0",
  "private": true,
  "description": "NOVAWEAR storefront, operations portal and API",
  "scripts": {
    "dev": "node scripts/dev.js",
    "setup": "npm --prefix BackEnd/server ci && npm --prefix client ci && npm --prefix admin ci",
    "check": "npm --prefix BackEnd/server test && npm --prefix client run build && npm --prefix admin run build"
  }
}
